# https://circleci.com/docs/2.0/configuration-reference/
version: 2
jobs:
  build:
    working_directory: ~/project
    docker:
      - image: alpine/helm:3.1.2
    steps:
      - checkout
      - run:
          command: mkdir /tmp/packaged_charts
      - run:
          name: Package all the charts
          command: |
            for chart in $(find ~/project/src -d -mindepth 1 -maxdepth 1)
              do helm package $chart -d /tmp/packaged_charts
            done
      - save_cache:
          paths:
            - /tmp/packaged_charts
          key: packaged_charts

  upload:
    docker:
      - image: circleci/golang:1.14.1
    steps:
      - checkout
      - restore_cache:
          key: packaged_charts
      - run:
          command: cp /tmp/packaged_charts ~/project/releases
      - run:
          name: Commit and push
          command: |
            git config --global user.email "infra-apps-uploader@blackhole.net"
            git config --global user.name "infra-apps Uploader"
            git add **/*.tgz
            git commit -m "CircleCI Helm chart upload"
            git status
          working_directory: /tmp/charts_repo
    # - run:
    #     name: Clone the Helm charts repository :-/
    #     command: |
    #       mkdir ~/.ssh
    #       echo -e "Host github.com\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
    #       git clone git@github.com:mkorejo/charts_repo.git /tmp/charts_repo

workflows:
  version: 2
  build_and_upload:
    jobs:
      - build:
          filters:
            branches:
              only: master
      - upload:
          requires:
            - build